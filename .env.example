# =============================================================================
# ReinventingAI Infrastructure Environment Configuration
# =============================================================================
#
# Copy this file to .env and update with your actual values
# NEVER commit .env files to version control
#
# Required vs Optional:
# - REQUIRED: Must be set for services to function
# - OPTIONAL: Has sensible defaults, can be customized
#

# =============================================================================
# N8N CONFIGURATION
# =============================================================================

# REQUIRED: Unique encryption key for n8n data encryption
# Generate with: openssl rand -hex 32
N8N_ENCRYPTION_KEY=your_64_character_encryption_key_here_replace_with_real_key

# REQUIRED: Domain where n8n will be accessible
# This must match your actual domain for OAuth callbacks to work
N8N_HOST=your-domain.com

# REQUIRED: Full webhook URL for external integrations
# Should match N8N_HOST with https protocol
WEBHOOK_URL=https://your-domain.com/

# OPTIONAL: Container and volume naming (defaults work for single environment)
N8N_CONTAINER_NAME=n8n-gabe
N8N_VOLUME_NAME=n8n_data_gabe

# OPTIONAL: Port mapping (external:internal)
N8N_EXTERNAL_PORT=5679
N8N_INTERNAL_PORT=5678

# OPTIONAL: Docker image version
N8N_IMAGE_VERSION=1.103.2

# =============================================================================
# BACKUP CONFIGURATION
# =============================================================================

# OPTIONAL: Directory for storing backups
# Default: $HOME/backups/n8n
BACKUP_DIR=$HOME/backups/n8n

# =============================================================================
# ENVIRONMENT SETTINGS
# =============================================================================

# OPTIONAL: Environment type (affects logging, debugging)
# Options: development, production
NODE_ENV=production

# OPTIONAL: Timezone for n8n
GENERIC_TIMEZONE=America/New_York

# =============================================================================
# N8N FEATURE FLAGS
# =============================================================================

# OPTIONAL: Enable/disable various n8n features
N8N_RUNNERS_ENABLED=true
N8N_DIAGNOSTICS_ENABLED=false
N8N_PUBLIC_API_SWAGGERUI_DISABLED=true
N8N_PUBLIC_API_DISABLED=true
N8N_VERSION_NOTIFICATIONS_ENABLED=false
N8N_PROTOCOL=https

# =============================================================================
# FUTURE ENVIRONMENTS
# =============================================================================
# 
# When adding dev/prod environments, prefix variables like:
# DEV_N8N_HOST=dev.your-domain.com
# PROD_N8N_HOST=your-domain.com
# 
# Then use: ${DEV_N8N_HOST} or ${PROD_N8N_HOST} in compose files
#